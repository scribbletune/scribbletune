module.exports = function(Scribbletune) {
	
	var Generate = function() {};

	/**
	 * Generate a clip based on a pattern and notes
	 * @return {[type]} [description]
	 */
	/*Generate.clip = function() {

	};*/

	Scribbletune.generate = Generate;
	
};